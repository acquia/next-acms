# This is a global configuration file for Drush to be used in
# our GitHub Actions CI environment.
command:
  site:
    install:
      options:
        db-url: 'sqlite://sites/default/files/.ht.sqlite'
        account-pass: admin

name: Container Image

on:
  workflow_dispatch:

jobs:
  build:
    name: Build container image and push it to Docker Hub
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: echo "Hello!"
